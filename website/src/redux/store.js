/*
import { configureStore } from '@reduxjs/toolkit'
import auth from './auth'

const rootReducer = {
  loginLogout: auth
}

export default configureStore({
  reducer: rootReducer,
  devTools: false,
})
*/
